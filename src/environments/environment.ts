export const environment = {
  production: false,
  apiUrl: '/api',
  // Déconnexion automatique après cette durée sans activité dans l'interface.
  // Abaissez-la (ex. 2) pour tester le comportement sans attendre.
  inactiviteMinutes: 30
};
